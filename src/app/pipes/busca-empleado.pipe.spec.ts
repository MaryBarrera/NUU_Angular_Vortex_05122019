import { BuscaEmpleadoPipe } from './busca-empleado.pipe';

describe('BuscaEmpleadoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscaEmpleadoPipe();
    expect(pipe).toBeTruthy();
  });
});
